runtime: nodejs20

instance_class: F2

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 15

env_variables:
  PORT: 8080

handlers:
- url: /.*
  static_files: dist/index.html
  upload: dist/index.html
- url: /static
  static_dir: dist/static
